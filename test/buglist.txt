glClear nechce zobrat string ako ostatne funkcie s konstantami, ale funguje spravne

glGenTextures
// void glGenTextures (GLsizei n, GLuint *textures)
static int luaglew_glGenTextures(lua_State *L) {
    GLuint return_value;
	glGenTextures((GLsizei)luaL_checklong(L, 1), &return_value);
    lua_pushinteger(L, return_value);
	return 1;
}
takto ale iba pre jednu, musi ich byt n !!!

pre n takto:
static int luaglew_glGenTextures(lua_State *L) {
    GLuint* return_value;
    GLsizei n = (GLsizei)luaL_checklong(L, 1);
    return_value = (GLuint*) malloc(n*sizeof(GLuint));
	glGenTextures(n, return_value);
    luaglew_pusharray_int(L, return_value);
	return 1;
}
cize pusharray funguje

glTexParameterf posledny parameter nejde ako string